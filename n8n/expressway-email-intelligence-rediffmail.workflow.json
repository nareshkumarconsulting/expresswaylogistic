{
  "name": "ExpressWay — AI Email Intelligence (Rediffmail Pro)",
  "nodes": [
    {
      "parameters": {
        "content": "## Rediffmail Pro / Enterprise inbox\n\nWebmail: https://webmail.rediffmailpro.com/\n\n1. On **IMAP Email Trigger** create **IMAP** credentials:\n   - User: **full email address** (not just the local part)\n   - Password: mailbox password (enter in n8n only — never commit)\n   - Host: `imap.rediffmailpro.com`\n   - Port: `993`\n   - SSL/TLS: **on**\n2. If 993 fails, retry port `143` with SSL **off** (legacy Rediff setting).\n3. Admin may need IMAP enabled: Admin → Other Services → Complete Mail Sync (set IMAP SYNC FROM date).\n4. Set `sourceAccount` in **Set Source Account** to this inbox address.\n5. Publish / activate. Existing Gmail workflows stay unchanged.\n\nDocs: https://support.rediffmailpro.com/article/imap-configuration-for-ms-outlook-365/",
        "height": 360,
        "width": 480,
        "color": 4
      },
      "id": "sticky-rediff-001",
      "name": "Rediffmail Setup",
      "type": "n8n-nodes-base.stickyNote",
      "typeVersion": 1,
      "position": [
        200,
        -220
      ]
    },
    {
      "parameters": {
        "mailbox": "INBOX",
        "postProcessAction": "nothing",
        "options": {
          "forceReconnect": 60
        }
      },
      "id": "imap-trigger-rediff-001",
      "name": "IMAP Email Trigger",
      "type": "n8n-nodes-base.emailReadImap",
      "typeVersion": 2,
      "position": [
        720,
        -40
      ]
    },
    {
      "parameters": {
        "mode": "manual",
        "duplicateItem": false,
        "assignments": {
          "assignments": [
            {
              "id": "src-rediff",
              "name": "sourceAccount",
              "value": "REPLACE_WITH_REDIFFMAIL_ADDRESS",
              "type": "string"
            }
          ]
        },
        "includeOtherFields": true,
        "options": {}
      },
      "id": "set-rediff-001",
      "name": "Set Source Account",
      "type": "n8n-nodes-base.set",
      "typeVersion": 3.4,
      "position": [
        960,
        -40
      ]
    },
    {
      "parameters": {
        "jsCode": "const item = $input.first().json;\n\nconst sourceAccount = item.sourceAccount;\nconst subject = item.subject || '(no subject)';\n\nlet senderEmail = item.from || '';\nlet senderName = '';\nconst match = String(senderEmail).match(/^(.*?)\\s*<([^>]+)>/);\nif (match) {\n  senderName = match[1].trim();\n  senderEmail = match[2];\n}\n\nconst messageId = item.messageId || item.uid || `${sourceAccount}-${Date.now()}`;\nconst receivedAt = new Date(item.date || Date.now()).toISOString();\nconst body = (item.textPlain || item.text || item.html || '').toString().slice(0, 12000);\n\nconst attachments = item.attachments || [];\nconst attachmentNames = attachments.map((a) => a.filename || a.name).filter(Boolean);\n\nreturn [\n  {\n    json: {\n      sourceAccount,\n      externalMessageId: messageId,\n      senderEmail: senderEmail || 'unknown@unknown.com',\n      senderName,\n      subject,\n      receivedAt,\n      body,\n      hasAttachments: attachmentNames.length > 0,\n      attachmentNames,\n    },\n  },\n];"
      },
      "id": "prepare-rediff-001",
      "name": "Prepare Email Context",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        1200,
        -40
      ]
    },
    {
      "parameters": {
        "jsCode": "const SYSTEM_PROMPT =\n  'You are an email intelligence assistant for ExpressWay Logistics. Classify into ONE category: shipment, quotation, alert, or general. Return JSON: { category, confidence, summary, urgency, extractedData }. Shipment fields: awb, trackingNo, pickup, destination, status, eta. Quotation: quoteNo, origin, destination, carrier, price, validity. Alert: alertType, urgency, requiredAction, deadline. General: sender, subject, date, summary.';\n\nconst email = $input.first().json;\n\nconst userContent = [\n  `From: ${email.senderName || ''} <${email.senderEmail}>`,\n  `Subject: ${email.subject}`,\n  `Date: ${email.receivedAt}`,\n  '',\n  email.body || '',\n].join('\\n');\n\nconst messages = [\n  { role: 'system', content: SYSTEM_PROMPT },\n  { role: 'user', content: userContent },\n];\n\nconst groqKey = $env.GROQ_API_KEY;\nconst geminiKey = $env.GEMINI_API_KEY;\nconst openaiKey = $env.OPENAI_API_KEY;\n\nlet response;\nlet provider = 'unknown';\n\nif (groqKey) {\n  provider = 'groq';\n  response = await this.helpers.httpRequest({\n    method: 'POST',\n    url: 'https://api.groq.com/openai/v1/chat/completions',\n    headers: {\n      Authorization: `Bearer ${groqKey}`,\n      'Content-Type': 'application/json',\n    },\n    body: {\n      model: 'llama-3.3-70b-versatile',\n      temperature: 0.1,\n      response_format: { type: 'json_object' },\n      messages,\n    },\n    json: true,\n  });\n} else if (geminiKey) {\n  provider = 'gemini';\n  const geminiRes = await this.helpers.httpRequest({\n    method: 'POST',\n    url: `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.0-flash:generateContent?key=${geminiKey}`,\n    headers: { 'Content-Type': 'application/json' },\n    body: {\n      contents: [{ parts: [{ text: `${SYSTEM_PROMPT}\\n\\n${userContent}` }] }],\n      generationConfig: {\n        temperature: 0.1,\n        responseMimeType: 'application/json',\n      },\n    },\n    json: true,\n  });\n  response = {\n    choices: [\n      {\n        message: {\n          content: geminiRes.candidates?.[0]?.content?.parts?.[0]?.text || '',\n        },\n      },\n    ],\n  };\n} else if (openaiKey) {\n  provider = 'openai';\n  response = await this.helpers.httpRequest({\n    method: 'POST',\n    url: 'https://api.openai.com/v1/chat/completions',\n    headers: {\n      Authorization: `Bearer ${openaiKey}`,\n      'Content-Type': 'application/json',\n    },\n    body: {\n      model: 'gpt-4o-mini',\n      temperature: 0.1,\n      response_format: { type: 'json_object' },\n      messages,\n    },\n    json: true,\n  });\n} else {\n  throw new Error(\n    'No AI key found. Add GROQ_API_KEY (free — console.groq.com) or GEMINI_API_KEY (free — aistudio.google.com) to .env.local and restart n8n.',\n  );\n}\n\nconst content = response.choices?.[0]?.message?.content || '';\nconst cleaned = content.replace(/```json\\n?/gi, '').replace(/```\\n?/g, '').trim();\n\nlet parsed;\ntry {\n  parsed = JSON.parse(cleaned);\n} catch {\n  parsed = {\n    category: 'general',\n    confidence: 0.4,\n    summary: 'Parse failed',\n    urgency: 'low',\n    extractedData: {},\n  };\n}\n\nparsed._aiProvider = provider;\n\nreturn [{ json: parsed }];"
      },
      "id": "ai-rediff-001",
      "name": "AI Classify & Extract",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        1440,
        -40
      ]
    },
    {
      "parameters": {
        "jsCode": "const email = $('Prepare Email Context').first().json;\nconst ai = $input.first().json;\n\nconst payload = {\n  sourceAccount: email.sourceAccount,\n  externalMessageId: email.externalMessageId,\n  senderEmail: email.senderEmail,\n  subject: email.subject,\n  receivedAt: email.receivedAt,\n  category: ai.category,\n  confidence: ai.confidence,\n  summary: ai.summary,\n  urgency: ai.urgency,\n  hasAttachments: email.hasAttachments,\n  attachmentNames: email.attachmentNames,\n  extractedData: ai.extractedData || {},\n};\n\nif (email.senderName) payload.senderName = email.senderName;\n\nreturn [{ json: payload }];"
      },
      "id": "build-rediff-001",
      "name": "Build Ingest Payload",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        1920,
        -40
      ]
    },
    {
      "parameters": {
        "method": "POST",
        "url": "={{ ($env.EXPRESSWAY_APP_URL || 'http://localhost:3000').replace(/\\/$/, '') + '/api/email-intelligence/ingest' }}",
        "sendHeaders": true,
        "headerParameters": {
          "parameters": [
            {
              "name": "Authorization",
              "value": "=Bearer {{ $env.EMAIL_INGEST_SECRET }}"
            },
            {
              "name": "Content-Type",
              "value": "application/json"
            }
          ]
        },
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={{ JSON.stringify($json) }}",
        "options": {}
      },
      "id": "post-rediff-001",
      "name": "POST ExpressWay Ingest",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        2160,
        -40
      ]
    }
  ],
  "connections": {
    "IMAP Email Trigger": {
      "main": [
        [
          {
            "node": "Set Source Account",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Set Source Account": {
      "main": [
        [
          {
            "node": "Prepare Email Context",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Prepare Email Context": {
      "main": [
        [
          {
            "node": "AI Classify & Extract",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Build Ingest Payload": {
      "main": [
        [
          {
            "node": "POST ExpressWay Ingest",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "AI Classify & Extract": {
      "main": [
        [
          {
            "node": "Build Ingest Payload",
            "type": "main",
            "index": 0
          }
        ]
      ]
    }
  },
  "pinData": {},
  "settings": {
    "executionOrder": "v1"
  },
  "staticData": null,
  "tags": [
    {
      "name": "expressway",
      "id": "tag-expressway"
    }
  ],
  "meta": {
    "templateCredsSetupCompleted": false
  },
  "active": false
}
